---
// Contact page
// Icon paths are from simpleicons.org (CC0). 24x24 viewBox.
const socials = [
  {
    name: 'Vimeo',
    url: 'https://vimeo.com/yrazielcom',
    icon: 'M23.9765 6.4168c-.105 2.338-1.739 5.5429-4.894 9.6089-3.2679 4.247-6.0301 6.3699-8.2861 6.3699-1.3969 0-2.578-1.2899-3.5429-3.8729-.646-2.369-1.2919-4.7399-1.9379-7.1119-.7189-2.5819-1.489-3.874-2.313-3.874-.1799 0-.8099.378-1.8899 1.1309l-1.129-1.457c1.186-1.0429 2.3549-2.0849 3.505-3.1279 1.5819-1.3629 2.7689-2.0829 3.5589-2.159 1.867-.1799 3.016 1.0959 3.447 3.8279.4689 2.948.793 4.7799.9769 5.495.5399 2.4499 1.1339 3.6739 1.7859 3.6739.504 0 1.2609-.7979 2.2719-2.3899 1.0099-1.5929 1.5499-2.8029 1.6219-3.6329.1439-1.3689-.3969-2.0549-1.6219-2.0549-.5759 0-1.1699.1319-1.7819.3929 1.1849-3.8819 3.4459-5.7709 6.7859-5.6619 2.4759.0739 3.6439 1.6799 3.5049 4.8159z',
  },
  {
    name: 'YouTube',
    url: 'https://youtube.com/user/yraziel',
    icon: 'M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z',
  },
  {
    name: 'Facebook',
    url: 'https://facebook.com/yraziel',
    icon: 'M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z',
  },
  {
    name: 'Instagram',
    url: 'https://instagram.com/yraziel',
    icon: 'M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324zM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8zm6.406-11.845a1.44 1.44 0 1 0 0 2.881 1.44 1.44 0 0 0 0-2.881z',
  },
  {
    name: 'LinkedIn',
    url: 'https://linkedin.com/in/yraziel',
    icon: 'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.063 2.063 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z',
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Contact — Yishay Raziel</title>
    <meta
      name="description"
      content="Get in touch with Yishay Raziel — music and sound for advertising."
    />
  </head>
  <body>
    <div class="page">
      <header class="site-header">
        <a href="/" class="logo-link" aria-label="Home">
          <img src="/logo.png" alt="Yishay Raziel — Music & Sound" class="logo" />
        </a>
        <nav class="top-nav" aria-label="Primary">
          <a href="/" class="nav-link" data-astro-prefetch>Works</a>
          <a href="/about" class="nav-link" data-astro-prefetch>About</a>
          <a href="/contact" class="nav-link is-active" data-astro-prefetch>Contact</a>
        </nav>
      </header>

      <main class="contact">
        <section class="intro">
          <h1>Contact me</h1>
          <p class="lead">Drop me a line — or give me a call.</p>
        </section>

        <section class="details">
          <div class="detail">
            <span class="label">Email</span>
            <a class="value" href="mailto:y@yraziel.com">y@yraziel.com</a>
          </div>
          <div class="detail">
            <span class="label">Phone</span>
            <a class="value" href="tel:+972584902902">0584-902-902</a>
          </div>
          <div class="detail">
            <span class="label">Studio</span>
            <span class="value">Tel Aviv, Israel</span>
          </div>
        </section>

        <form
          class="contact-form"
          name="contact"
          method="POST"
          action="https://formsubmit.co/y@yraziel.com"
        >
          <input type="hidden" name="_subject" value="New message from yraziel.com" />
          <input type="hidden" name="_captcha" value="false" />
          <input type="hidden" name="_next" value="/contact?sent=1" />
          <div class="field">
            <label for="name">Your name</label>
            <input id="name" name="name" type="text" required autocomplete="name" />
          </div>
          <div class="field">
            <label for="email">Your email</label>
            <input id="email" name="email" type="email" required autocomplete="email" />
          </div>
          <div class="field">
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="5" required></textarea>
          </div>
          <button type="submit" class="send-btn">Send</button>
          <p class="sent-note" id="sent-note" hidden>Thanks — your message is on its way.</p>
        </form>

        <section class="socials">
          <span class="label">Also here</span>
          <ul>
            {socials.map((s) => (
              <li>
                <a href={s.url} target="_blank" rel="noopener noreferrer" aria-label={s.name}>
                  <svg class="social-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                    <path d={s.icon} fill="currentColor" />
                  </svg>
                  <span>{s.name}</span>
                </a>
              </li>
            ))}
          </ul>
        </section>
      </main>

      <footer>
        <p>© {new Date().getFullYear()} Yishay Raziel Music for Advertising Ltd.</p>
      </footer>
    </div>

    <style>
      :root {
        --bg: #ffffff;
        --fg: #000000;
        --muted: #7a7a7a;
        --border: #e5e5e5;
        --ease: cubic-bezier(0.4, 0, 0.2, 1);
      }
      * { box-sizing: border-box; }
      html, body { margin: 0; padding: 0; }
      body {
        background: var(--bg);
        color: var(--fg);
        font-family: -apple-system, BlinkMacSystemFont, 'Inter', system-ui, sans-serif;
        font-weight: 300;
        letter-spacing: 0.01em;
        animation: fade-in 0.35s var(--ease) both;
      }
      @keyframes fade-in { from { opacity: 0 } to { opacity: 1 } }
      .page {
        animation: page-scale-in 0.4s var(--ease) both;
      }
      @keyframes page-scale-in {
        from { transform: scale(0.98); }
        to   { transform: scale(1); }
      }

      /* Header */
      .site-header { padding: 3rem 2rem 1.5rem; text-align: center; position: relative; }
      .logo-link { display: inline-block; text-decoration: none; }
      .logo {
        width: clamp(180px, 22vw, 260px);
        height: auto;
        display: inline-block;
        transition: transform 0.6s var(--ease);
      }
      .logo-link:hover .logo { transform: scale(1.02); }
      .top-nav { display: flex; gap: 1.8rem; justify-content: center; margin-top: 1.2rem; }
      .nav-link {
        font-size: 0.8rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        text-decoration: none;
        color: var(--muted);
        padding: 0.25rem 0.1rem;
        border-bottom: 1px solid transparent;
        transition: color 0.3s var(--ease), border-color 0.3s var(--ease);
      }
      .nav-link:hover { color: var(--fg); }
      .nav-link.is-active { color: var(--fg); border-bottom-color: var(--fg); }

      /* Contact layout */
      .contact {
        max-width: 720px;
        margin: 3rem auto 6rem;
        padding: 0 2rem;
      }
      .intro h1 {
        margin: 0 0 0.4rem;
        font-size: clamp(1.8rem, 3.5vw, 2.6rem);
        font-weight: 300;
        letter-spacing: 0.02em;
      }
      .intro .lead {
        margin: 0 0 2.5rem;
        color: var(--muted);
        font-size: 1rem;
      }

      .details {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: 1.5rem 2.5rem;
        padding: 1.8rem 0;
        border-top: 1px solid var(--border);
        border-bottom: 1px solid var(--border);
        margin-bottom: 3rem;
      }
      .detail { display: flex; flex-direction: column; gap: 0.3rem; }
      .label {
        font-size: 0.7rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--muted);
      }
      .value {
        font-size: 1rem;
        color: var(--fg);
        text-decoration: none;
        transition: opacity 0.25s var(--ease);
      }
      a.value {
        border-bottom: 1px solid transparent;
        padding-bottom: 1px;
      }
      a.value:hover { border-bottom-color: var(--fg); }

      /* Form */
      .contact-form {
        display: grid;
        gap: 1.2rem;
        margin-bottom: 3.5rem;
      }
      .field { display: flex; flex-direction: column; gap: 0.4rem; }
      .field label {
        font-size: 0.7rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--muted);
      }
      .field input,
      .field textarea {
        font: inherit;
        font-size: 0.95rem;
        color: var(--fg);
        background: transparent;
        border: 0;
        border-bottom: 1px solid var(--border);
        padding: 0.6rem 0.1rem;
        transition: border-color 0.3s var(--ease);
        outline: none;
        resize: vertical;
      }
      .field input:focus,
      .field textarea:focus {
        border-bottom-color: var(--fg);
      }
      .send-btn {
        justify-self: start;
        margin-top: 0.5rem;
        font: inherit;
        font-size: 0.8rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--fg);
        background: transparent;
        border: 1px solid var(--fg);
        padding: 0.75rem 2.2rem;
        cursor: pointer;
        transition: background 0.3s var(--ease), color 0.3s var(--ease);
      }
      .send-btn:hover { background: var(--fg); color: var(--bg); }
      .sent-note {
        margin: 0.5rem 0 0;
        font-size: 0.85rem;
        color: var(--muted);
      }

      /* Socials */
      .socials {
        display: flex;
        flex-direction: column;
        gap: 0.8rem;
        padding-top: 2rem;
        border-top: 1px solid var(--border);
      }
      .socials ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        gap: 0.4rem 1.4rem;
      }
      .socials a {
        font-size: 0.85rem;
        color: var(--fg);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.45rem;
        padding-bottom: 1px;
        transition: opacity 0.25s var(--ease);
      }
      .socials a span {
        border-bottom: 1px solid transparent;
        padding-bottom: 1px;
        transition: border-color 0.25s var(--ease);
      }
      .socials a:hover span { border-bottom-color: var(--fg); }
      .social-icon {
        width: 16px;
        height: 16px;
        flex-shrink: 0;
        display: inline-block;
      }

      /* Reveal on load */
      .intro, .details, .contact-form, .socials {
        opacity: 0;
        transform: translateY(14px);
        animation: rise 0.7s var(--ease) forwards;
      }
      .intro { animation-delay: 0.15s; }
      .details { animation-delay: 0.3s; }
      .contact-form { animation-delay: 0.45s; }
      .socials { animation-delay: 0.6s; }
      @keyframes rise {
        to { opacity: 1; transform: translateY(0); }
      }
      @media (prefers-reduced-motion: reduce) {
        .intro, .details, .contact-form, .socials { animation: none; opacity: 1; transform: none; }
      }

      footer {
        text-align: center;
        padding: 2rem;
        color: var(--muted);
        font-size: 0.8rem;
        border-top: 1px solid var(--border);
      }
    </style>

    <script is:inline data-astro-rerun>
      const fadein = () => document.body.classList.add('js-fadein');
      fadein();
      document.addEventListener('astro:page-load', fadein);
      window.addEventListener('pageshow', fadein);
      document.addEventListener('astro:page-load', () => {
        if (new URLSearchParams(location.search).get('sent') === '1') {
          const note = document.getElementById('sent-note');
          if (note) note.hidden = false;
          history.replaceState(null, '', '/contact');
        }
      });
    </script>
  </body>
</html>
